<resources>

    <style name="StyleDialog.Dark" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item> <!-- Transparent background -->
        <item name="android:background">@color/white</item> <!-- White background -->
        <item name="android:windowIsFloating">true</item> <!-- Floating style -->
    </style>
</resources>